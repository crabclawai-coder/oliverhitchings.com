{
  "name": "oliverhitchings-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "start": "sirv dist --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^6.1.7",
    "sirv-cli": "^3.0.1"
  },
  "devDependencies": {}
}
